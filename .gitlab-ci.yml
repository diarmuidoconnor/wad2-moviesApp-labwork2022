# Node docker image with Cypress and Chrome pre-installed
image: node:latest

# Pipeline
stages:
  - install
  - build
  - test

# Jobs 

install_dependencies:
  stage: install
  script:
    - npm ci
  artifacts:
    paths:
      - node_modules/

bundle_app:
  stage: build
  script:
    - npm run build